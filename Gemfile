# this may no longer be accurate.. the cap repo at the time had a bug fix I required
group :develpment do
  gem 'capistrano', '~> 3.0', git: 'https://github.com/capistrano/capistrano.git', require: false
  gem 'capistrano-bundler'
  gem 'capistrano-rvm', '~> 0.0.3'
  gem 'capistrano-rails'
end

group :production do
  gem 'unicorn'
end